/** Single outer container for all landing sections (Hero → Footer). */
export const LANDING_CONTAINER = 'mx-auto w-full max-w-7xl px-6';

/** Optional inner column for centered copy — same max width on every section. */
export const LANDING_CONTENT = 'mx-auto w-full max-w-3xl';
