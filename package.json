{
  "name": "cursor-project",
  "version": "0.1.0",
  "private": true,
  "description": "Production-ready AI SaaS starter kit monorepo",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "start": "pnpm --filter web start",
    "lint": "pnpm --filter web lint",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,md,css}\"",
    "format:check": "prettier --check \"**/*.{js,jsx,ts,tsx,json,md,css}\"",
    "audit:i18n": "node scripts/audit-i18n-coverage.mjs",
    "audit:i18n:strict": "node scripts/audit-i18n-coverage.mjs --strict"
  },
  "devDependencies": {
    "@repo/config": "workspace:*",
    "next": "^15.3.3",
    "prettier": "^3.5.3",
    "prettier-plugin-tailwindcss": "^0.6.11"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
